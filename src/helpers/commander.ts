import { Command } from 'commander'
/**
 * Commander Program - creates an instance of commander to be used in the entire application
 * @returns command
 */
const program = new Command()

export default program
